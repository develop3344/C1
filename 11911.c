/* 判断n位数（n>=3）是否为 水仙花数 */
# include<stdio.h>
int main (void)
{
    int n,sum=0,term，tmp；
    printf("Enter n:\n");
    scanf("%d",&n);
    
    for()
    while(n>0)
    {
        term= tmp%10;
        sum += term;
        n= n/10;
    }

    printf("Sum is %d\n",sum);
    return 0;
}
